---
- name: Linux Server side Java CPU
  hosts: linux_node
  diff: true 
  serial: 1
  gather_facts: false
  vars_files: 
    - vars.yaml
  tasks:
    - name: calling packaging tasks
      include: java_server_cpu_build.yaml
      vars:
        packing_path: "/home/cloud_user/packing"
        packed_path: "/home/cloud_user/packed"
        os: "linux"
        name: "{{ item.name }}"
        java: "{{ item.java }}"
        vendor: "{{ item.vendor }}"
        version: "{{ item.ver }}"
        artifactory_push: "{{ item.artifactory_push }}"
        fonts: "{{ item.push }}"
        benchmark: "{{ item.benchmark }}" 
      with_items: 
       - "{{ packages_linux }}"

- name: Solaris Server side Java CPU
  hosts: solaris_node
  vars_files: 
    - ./vars.yaml
  tasks:
    - name: calling packaging tasks 
      include: java_server_cpu_build.yaml
      vars:
        packing_path: "/home/clous_user/packing"
        packed_path: "/home/cloud_user/packed"
        os: "linux"
        name: "{{ item.name }}"
        java: "{{ item.java }}"
        vendor: "{{ item.vendor }}"
        version: "{{ item.ver }}"
        artifactory_push: "{{ item.artifactory_push }}"
        fonts: "{{ item.push }}"
        benchmark: "{{ item.benchmark }}" 
      with_items: 
       - "{{ packages_solaris }}"

- name: Solaris Sparc side Java CPU
  hosts: sparc_node
  vars_files: 
    - ./vars.yaml
  tasks:
    - name: calling packaging tasks 
      include: java_server_cpu_build.yaml
      vars:
        packing_path: "/home/clous_user/packing"
        packed_path: "/home/cloud_user/packed"
        os: "linux"
        name: "{{ item.name }}"
        java: "{{ item.java }}"
        vendor: "{{ item.vendor }}"
        version: "{{ item.ver }}"
        artifactory_push: "{{ item.artifactory_push }}"
        fonts: "{{ item.push }}"
        benchmark: "{{ item.benchmark }}" 
      with_items: 
       - "{{ packages_sparc }}"
