---
common:
  - mlocate
  - bzip2
  - gzip
  - tcp_wrappers
  - openssh
  - firewalld
  - fail2ban
  - rsync
  - sendmail
  - python3
  - python3-pip
  - logrotate
  - auditd
  - cron
  - at

dev_tools:
   Default:
     - git
     - tree
     - curl
     - wget
   RedHat:
     - git
     - tree
     - curl
     - wget
     - httpd
   Debian:
     - git
     - tree
     - curl
     - wget
     - apache2

ops_tools:
   Default:
     - htop
     - dstat
     - tmux
   RedHat:
     - sysstat
     - htop
     - dstat
     - tmux
   Debian:
     - sysstat
     - htop
     - dstat
     - tmux

# python  packages are not platform rhel/debian specific.
# generic python scripting and development packages
misc_py:
   Default:
     - requests
     - beautifulsoup4
     - scrapy
     - nose
     - sh
     - dateutils
     - psutil
     - pyquery

# python packages for aws interaction
aws_py:
  Default:
    - requests
    - schedule
    - boto3
    - pyyaml
    - awscli

# python packages required for google cloud platform
google_py:
  Default:
    - requests
    - schedule
    - google-auth
    - google-cloud-storage
    - google-cloud-pubsub

azure_py:
  Default:
    - azure-storage-blob
    - azure-identity
    - azure-servicebus
    
# python web application framework
flask_py:
   Default:
    - flask
    - SQLAlchemy
    - gunicorn
    - pyyaml
    - requests
    - dash
    - plotly

# python Artificial Intelligence  and Machine Learning packages
aiml_py:
   Default:
    - pandas
    - numpy
    - scipy
    - scikit-learn
    - matplotlib
